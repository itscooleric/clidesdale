{
  "dales": {
    "edge": {
      "host": "203.0.113.10",
      "user": "deploy",
      "key": "~/.ssh/sdale",
      "session": "work",
      "docker_user": "docker-ops",
      "docker_blacklist": ["critical-db", "vault"],
      "allowed_operators": ["sentinel", "lead", "dubs", "larry"],
      "operator_tiers": {
        "full": ["sentinel", "lead"],
        "readonly": ["dubs", "larry"]
      },
      "mode": "supervised"
    }
  },
  "defaults": {
    "key": "~/.ssh/sdale",
    "exclude": ["node_modules", ".git"]
  }
}
